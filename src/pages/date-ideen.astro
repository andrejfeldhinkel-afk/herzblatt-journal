---
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogCard from '../components/BlogCard.astro';
import AppCTA from '../components/AppCTA.astro';
import { getCollection } from 'astro:content';

const title = 'Date-Ideen 2026: 100+ kreative Ideen für unvergessliche Dates';
const description = 'Die besten Date-Ideen für jede Situation: Erste Dates, Outdoor, Zuhause, romantisch, abenteuerlich oder günstig. Über 100 Ideen für Paare & Singles.';
const keywords = ['date ideen', 'date ideen zuhause', 'erstes date ideen', 'romantische date ideen', 'ausgefallene date ideen', 'date ideen winter', 'date ideen sommer'];

const allPosts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => (b.data.date?.valueOf?.() || 0) - (a.data.date?.valueOf?.() || 0));

function getPostsByTags(tags: string[]) {
  const tagSet = new Set(tags.map(t => t.toLowerCase()));
  return allPosts
    .filter(p => p.data.tags.some((t: string) => tagSet.has(t.toLowerCase())))
    .slice(0, 6);
}

function getReadingTime(body: string) {
  return `${Math.ceil(body.split(/\s+/).length / 200)} Min. Lesezeit`;
}

const categories = [
  {
    id: 'erste-dates',
    emoji: '☕',
    title: 'Ideen fürs erste Date',
    description: 'Beim ersten Treffen zählt eine entspannte Atmosphäre. Diese Ideen sorgen für gute Gespräche ohne Druck.',
    tags: ['erstes date', 'kennenlernen', 'dating-tipps'],
    quickIdeas: ['Kaffee in einem gemütlichen Café', 'Spaziergang im Park', 'Street-Food-Markt besuchen', 'Eisdiele & Stadtbummel', 'Museum mit freiem Eintritt', 'Flohmarkt zusammen stöbern'],
  },
  {
    id: 'romantisch',
    emoji: '🌹',
    title: 'Romantische Date-Ideen',
    description: 'Wenn es kribbeln soll: Diese Dates schaffen eine besondere Atmosphäre für Verliebte.',
    tags: ['romantik', 'date-ideen', 'quality time'],
    quickIdeas: ['Sonnenuntergang am Wasser', 'Candle-Light-Dinner kochen', 'Sternegucken auf dem Land', 'Gondel- oder Bootsfahrt', 'Weinprobe zu zweit', 'Liebesbrief schreiben & vorlesen'],
  },
  {
    id: 'zuhause',
    emoji: '🏠',
    title: 'Date-Ideen für Zuhause',
    description: 'Kein Budget oder schlechtes Wetter? Kein Problem! Die besten Dates finden manchmal auf der Couch statt.',
    tags: ['quality time', 'alltag', 'date-ideen'],
    quickIdeas: ['Gemeinsam ein neues Rezept kochen', 'Film-Marathon mit Snacks', 'Brettspiel-Abend', 'Gemeinsam malen oder basteln', 'Spa-Abend mit Masken & Massage', 'Blind-Tasting Abend'],
  },
  {
    id: 'abenteuer',
    emoji: '🧗',
    title: 'Abenteuerliche & Aktive Dates',
    description: 'Gemeinsame Abenteuer schweißen zusammen. Diese Dates bringen Adrenalin und Endorphine.',
    tags: ['abenteuer', 'outdoor', 'date-ideen', 'aktivitäten'],
    quickIdeas: ['Kletterhalle oder Bouldern', 'Kanu oder SUP fahren', 'Escape Room zu zweit', 'Geocaching/Schnitzeljagd', 'Tanzkurs ausprobieren', 'Nachtwanderung mit Fackeln'],
  },
  {
    id: 'langzeitpaare',
    emoji: '💑',
    title: 'Für Langzeitpaare',
    description: 'Nach Jahren zusammen braucht es frische Impulse. Diese Ideen bringen den Funken zurück.',
    tags: ['langzeitbeziehung', 'beziehung', 'beziehungstipps'],
    quickIdeas: ['Erstes Date nachspielen', 'Bucket-List erstellen', 'Überraschungs-Date planen', 'Neues Hobby zusammen starten', 'Wochenend-Trip ohne Handy', 'Love-Letter-Challenge: 7 Tage, 7 Briefe'],
  },
  {
    id: 'kostenlos',
    emoji: '🆓',
    title: 'Kostenlose Date-Ideen',
    description: 'Die besten Dinge im Leben sind kostenlos — das gilt auch für Dates. Hier sind Ideen die nichts kosten.',
    tags: ['dating-tipps', 'date-ideen', 'kennenlernen'],
    quickIdeas: ['Picknick im Park (von Zuhause)', 'Fotowalk durch die Stadt', 'Bibliothek besuchen & vorlesen', 'Sonnenaufgang zusammen anschauen', 'Gemeinsam Yoga im Park', 'Stadtteile erkunden die ihr nicht kennt'],
  },
];

const breadcrumbs = [
  { name: 'Home', url: '/' },
  { name: 'Date-Ideen', url: '/date-ideen' },
];
---

<BaseLayout
  title={title}
  description={description}
  keywords={keywords}
  breadcrumbs={breadcrumbs}
  faq={[
    { question: 'Was sind gute Ideen für ein erstes Date?', answer: 'Die besten ersten Dates sind entspannt und ermöglichen echte Gespräche: ein Café, ein Spaziergang im Park, ein Street-Food-Markt oder ein Museum. Vermeide Kino (kein Gespräch möglich) und zu formelle Restaurants (zu viel Druck).' },
    { question: 'Welche Date-Ideen kosten nichts?', answer: 'Viele tolle Dates sind kostenlos: Picknick im Park, Fotospaziergang, Sonnenuntergang am Wasser, gemeinsames Kochen mit vorhandenen Zutaten, Sternegucken, Bibliotheksbesuch oder ein Stadtspaziergang durch neue Viertel.' },
    { question: 'Was kann man als Paar Zuhause machen?', answer: 'Zuhause-Dates können sehr kreativ sein: gemeinsam ein neues Rezept kochen, Blind-Tasting, Spieleabend, Spa-Abend mit Gesichtsmasken, gemeinsam malen, ein Fotobuch erstellen oder einen Themenabend gestalten (z.B. italienischer Abend mit Pasta und Film).' },
  ]}
>
  <!-- Hero -->
  <section class="relative overflow-hidden" style="background: linear-gradient(135deg, #e11d48 0%, #be185d 50%, #9f1239 100%);">
    <div class="absolute inset-0" style="background: radial-gradient(circle at 20% 30%, rgba(255,255,255,0.12) 0%, transparent 50%);"></div>
    <div class="absolute inset-0" style="background: radial-gradient(circle at 80% 70%, rgba(255,255,255,0.08) 0%, transparent 50%);"></div>
    <div class="max-w-4xl mx-auto px-4 sm:px-6 py-16 sm:py-24 text-center relative">
      <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-xs font-semibold mb-6" style="background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.2); backdrop-filter: blur(8px);">
        🎯 Über 100 Ideen · nach Situation sortiert
      </div>
      <h1 class="text-3xl sm:text-5xl lg:text-6xl font-extrabold text-white mb-6" style="font-family: var(--font-serif); letter-spacing: -0.02em; text-shadow: 0 2px 20px rgba(0,0,0,0.2);">
        Date-Ideen für<br/>jede Situation
      </h1>
      <p class="text-lg sm:text-xl max-w-2xl mx-auto mb-8" style="color: rgba(255,255,255,0.9);">
        Erstes Date, Regen-Date, Zuhause-Date oder Abenteuer — finde die perfekte Idee für euren nächsten Moment zu zweit.
      </p>
      <div class="flex flex-wrap justify-center gap-2">
        {categories.map(c => (
          <a href={`#${c.id}`} class="inline-flex items-center gap-1.5 px-4 py-2 rounded-full text-sm font-medium transition-all hover:-translate-y-0.5" style="background: rgba(255,255,255,0.15); color: white; border: 1px solid rgba(255,255,255,0.2); backdrop-filter: blur(8px);">
            <span>{c.emoji}</span> {c.title.split(' ').slice(-2).join(' ')}
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Stats-Band -->
  <section style="background: var(--bg-soft); border-bottom: 1px solid var(--border); padding: 2rem 1rem;">
    <div class="max-w-5xl mx-auto grid grid-cols-2 sm:grid-cols-4 gap-6 text-center">
      <div>
        <div class="text-3xl font-extrabold" style="color: var(--color-primary-600); font-family: var(--font-serif);">100+</div>
        <div class="text-xs font-medium mt-1" style="color: var(--text-muted);">Date-Ideen</div>
      </div>
      <div>
        <div class="text-3xl font-extrabold" style="color: var(--color-primary-600); font-family: var(--font-serif);">6</div>
        <div class="text-xs font-medium mt-1" style="color: var(--text-muted);">Kategorien</div>
      </div>
      <div>
        <div class="text-3xl font-extrabold" style="color: var(--color-primary-600); font-family: var(--font-serif);">12</div>
        <div class="text-xs font-medium mt-1" style="color: var(--text-muted);">Städte im Guide</div>
      </div>
      <div>
        <div class="text-3xl font-extrabold" style="color: var(--color-primary-600); font-family: var(--font-serif);">0 €</div>
        <div class="text-xs font-medium mt-1" style="color: var(--text-muted);">viele kostenlos</div>
      </div>
    </div>
  </section>

  <div class="max-w-5xl mx-auto px-4 sm:px-6 py-16">
    {categories.map((cat, idx) => {
      const posts = getPostsByTags(cat.tags);
      return (
        <section id={cat.id} class="mb-16 scroll-mt-24">
          <div class="flex items-start gap-4 mb-6">
            <div class="shrink-0 w-12 h-12 rounded-2xl flex items-center justify-center text-2xl" style="background: var(--color-primary-50); border: 1px solid var(--color-primary-100);">
              {cat.emoji}
            </div>
            <div>
              <h2 class="text-2xl font-extrabold mb-2" style="color: var(--text); font-family: var(--font-serif);">{cat.title}</h2>
              <p class="text-sm" style="color: var(--text-muted);">{cat.description}</p>
            </div>
          </div>

          <!-- Quick Ideas Grid -->
          <div class="grid grid-cols-2 sm:grid-cols-3 gap-3 mb-8">
            {cat.quickIdeas.map((idea, i) => (
              <div class="rounded-xl p-4 text-center transition-all hover:-translate-y-0.5" style="background: var(--bg-soft); border: 1px solid var(--border);">
                <p class="text-sm font-medium" style="color: var(--text);">{idea}</p>
              </div>
            ))}
          </div>

          <!-- Related Blog Posts -->
          {posts.length > 0 && (
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
              {posts.slice(0, 3).map(p => (
                <BlogCard
                  title={p.data.title}
                  description={p.data.description}
                  date={p.data.date}
                  tags={p.data.tags}
                  slug={p.id}
                  readingTime={getReadingTime(p.body || '')}
                  image={p.data.image}
                  imageAlt={p.data.imageAlt}
                />
              ))}
            </div>
          )}
          {idx < categories.length - 1 && <hr class="mt-12" style="border-color: var(--border);" />}
        </section>
      );
    })}

    <AppCTA variant="inline" />

    <!-- Weiter Lesen: Related Guides -->
    <section class="mt-16 pt-10" style="border-top: 1px solid var(--border);">
      <div class="text-center mb-10">
        <h2 class="text-2xl sm:text-3xl font-extrabold mb-3" style="color: var(--text); font-family: var(--font-serif);">Passende Guides für dich</h2>
        <p class="text-base" style="color: var(--text-muted);">Wer Date-Ideen sucht, braucht oft auch diese Guides.</p>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <a href="/erstes-date-guide" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">💫</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Erstes-Date-Guide</h3>
          <p class="text-sm" style="color: var(--text-muted);">Alles was du wissen musst, um dein erstes Date perfekt zu machen.</p>
        </a>
        <a href="/flirten-lernen-guide" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">✨</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Flirten lernen</h3>
          <p class="text-sm" style="color: var(--text-muted);">Körpersprache, Smalltalk und charmant durch den ersten Eindruck.</p>
        </a>
        <a href="/kommunikation/" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">💬</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Kommunikation</h3>
          <p class="text-sm" style="color: var(--text-muted);">So führt ihr tiefe Gespräche beim Date (und danach).</p>
        </a>
        <a href="/dating-typ-test" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">🎯</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Dating-Typ Test</h3>
          <p class="text-sm" style="color: var(--text-muted);">Finde in 2 Min heraus, welche Date-Ideen zu dir passen.</p>
        </a>
        <a href="/online-dating-guide" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">📱</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Online-Dating</h3>
          <p class="text-sm" style="color: var(--text-muted);">Vom Match zum Date: der komplette Online-Dating-Guide.</p>
        </a>
        <a href="/beziehungsratgeber" class="group rounded-2xl p-6 transition-all hover:-translate-y-1 hover:shadow-lg" style="background: var(--bg-card); border: 1px solid var(--border); text-decoration: none; color: inherit;">
          <div class="text-3xl mb-3">💑</div>
          <h3 class="text-lg font-bold mb-2" style="color: var(--color-primary-600); font-family: var(--font-serif);">Beziehungsratgeber</h3>
          <p class="text-sm" style="color: var(--text-muted);">Für wenn aus dem Date mehr wird.</p>
        </a>
      </div>
    </section>

    <!-- City Links -->
    <section class="mt-16 pt-10" style="border-top: 1px solid var(--border);">
      <h2 class="text-xl font-bold mb-4" style="color: var(--text); font-family: var(--font-serif);">Date-Spots in deiner Stadt</h2>
      <div class="flex flex-wrap gap-2">
        {['Berlin', 'München', 'Hamburg', 'Köln', 'Frankfurt', 'Stuttgart', 'Düsseldorf', 'Leipzig', 'Dresden', 'Nürnberg', 'Freiburg', 'Münster'].map(city => (
          <a href={`/dating-in/${city.toLowerCase().replace('ü', 'ue')}`} class="text-xs font-medium px-3 py-1.5 rounded-full transition-all hover:-translate-y-0.5" style="background: var(--bg-soft); color: var(--text-muted); border: 1px solid var(--border);">
            {city}
          </a>
        ))}
      </div>
    </section>
  </div>
</BaseLayout>
